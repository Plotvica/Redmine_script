{
  "name": "redmine-dashboard-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Local configurable Redmine dashboards.",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-js.js"
  },
  "engines": {
    "node": ">=20"
  }
}
